{% extends 'base.html.twig' %}

{% block title %}Modifier Plat{% endblock %}

{% block body %}
    <h1 class="text-center">Modifier Plat</h1>

    <div class="container">

        <div class="d-grid gap-2">
            {{ include('plat/_form.html.twig', {'button_label': 'Modifier'}) }}
            <a class="btn btn-outline-primary" role="button" href="{{ path('plat_index') }}">Retour à la liste</a>
            {{ include('plat/_delete_form.html.twig') }}
        </div>

    </div>
{% endblock %}
